---
description: "009 — Independent Verification Agent. Runtime audit, DOC-CHECK second pass, Registry validation, FULL-BASE drift. Read-only — never edits."
mode: all
permission:
  read:
    "src/**": allow
    "docs/**": allow
    "agents/**": allow
  edit:
    "src/**": deny
    "docs/**": allow
  bash:
---
