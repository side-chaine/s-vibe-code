---
# ═══════════════════════════════════════════════════
# 007 — Scout & Context Packer
# Model: fast/cheap LLM (DeepSeek recommended)
# ═══════════════════════════════════════════════════
# Проверь свои модели: opencode models
#
# Рекомендации:
# - чтобы не тратить токены: opencode-go/deepseek-v4-flash
# - для точности: opencode-go/kimi-k2.6
#
# Если model не указан — используется default OpenCode
# ═══════════════════════════════════════════════════

description: "007 — Scout. Context packing, code recon, MACRO/MICRO packs, DOC-CHECK, subagent dispatch."
model: opencode-go/deepseek-v4-flash
mode: all
permission:
  read:
    "src/**": allow
    "docs/**": allow
    "agents/**": allow
  edit:
    "*.md": allow
    "agents/**": allow
  bash:
---
